apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${name}-pvc
  namespace: game
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs
  resources:
    requests:
      storage: 80Gi